# watch小筆記

watch的第一個參數可以是任意的數據來源，所以可以使用ref來取用基本型別，或者是使用reactive的方式來用物件的方式。

深層的watch代表著需要遍歷所有對象中的屬性，所以當使用在大型數資料的時候，會很耗費資源，不建議使用，必要時才能用。

要停止要一個watch，手動的話就只要把它回傳的函式給執行返回就好了。

需要非同步的watch的狀況很少，儘量使用條件式的偵測邏輯。